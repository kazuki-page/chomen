# 家主の帳面

賃貸マンションの入居者管理・修繕管理アプリ。

実家で所有する賃貸マンション（1棟40室・駐車場5台）の管理業務を対象とした Web アプリケーション（PWA）。
現在 Notion で運用している業務を置き換えることを目的とする。

**デモ**: https://chomen-demo.kazuki.page （`demo@example.com` / `demo1234`）

架空のデータが入った、自由に触れる環境。入力欄はあらかじめ埋めてある。
書き込みも削除もできて、毎日 朝4時ごろに初期状態へ戻る。
アカウントに関わる操作（パスワード変更・招待）だけは画面を残して動作を止めてある。

## 解決したい課題

現行の Notion 運用には次の課題がある。

- データベースビューが起点のため、**「今何をすべきか」を画面が教えてくれない**（主な操作者は50代）
- 正しい操作に**運用マニュアルの参照が必要**
- 入居確定時に空室を「過去」にする、入居完了時に更新レコードを作る等、**付随作業がすべて人力**
- 同じ「ステータス」列に `進行中/完了/未/準備中/現在/過去` が混在し、**意味が文脈依存**
- **修繕案件の進行状況が追えず、放置に気づけない**

## アプローチ

- 運用マニュアルの手順を**アプリ側に埋め込み、マニュアルを不要にする**
- 「空室」「現在の家賃」を保存せず**導出値**とし、手作業の連鎖を消す
- 修繕は工程を細分化せず、**「誰がやる」「今誰待ち」**の2点と放置検知に絞る

## 技術スタック

| レイヤー | 採用 |
|---|---|
| フレームワーク | React Router v8（framework mode） |
| 実行環境 | Cloudflare Workers |
| ビルド | Vite + `@cloudflare/vite-plugin` |
| スタイル | Tailwind CSS v4 |
| DB | Cloudflare D1 (SQLite) + Drizzle ORM |
| ストレージ | Cloudflare R2 |
| 認証 | Better Auth（招待制） |

## ドキュメント

| 文書 | 内容 |
|---|---|
| [要件定義](docs/requirements.md) | 背景・スコープ・ドメインモデル・画面構成・開発フェーズ |
| [データモデル](docs/data-model.md) | テーブル定義・自動化ルール・テナント分離方針 |

## 開発

```bash
npm install
npm run dev
```

| コマンド | 内容 |
|---|---|
| `npm run dev` | 開発サーバー |
| `npm run build` | ビルド |
| `npm run typecheck` | 型チェック |
| `npm run deploy:demo` | デモ環境へデプロイ |
| `npm run deploy` | 本番へデプロイ |

### 環境

本番とデモは**同じコードを配るだけ**で、リポジトリもブランチも1つ。
差分は環境変数 `DEMO_MODE` だけに閉じ込めてあり、ここに条件分岐を増やさない。

| | 本番 | デモ |
|---|---|---|
| URL | 家族用のため非公開 | chomen-demo.kazuki.page |
| Worker | `oyasan-app` | `chomen-demo` |
| D1 | `oyasan-app-db` | `chomen-demo-db` |
| メール送信 | あり | なし（キーを登録しない） |
| 検索エンジン | 拒否 | 許可 |

**デプロイは必ず デモ → 本番 の順に行う。** デモが本番の予行演習になる。
スキーマを変えたときは、それぞれのデータベースに移行を当てる必要がある。

```bash
npm run db:migrate:demo    # デモの D1 に移行を適用
npm run deploy:demo        # デモで確認してから
npm run db:migrate:remote  # 本番の D1 に移行を適用
npm run deploy             # 本番へ
```

デモのデータは cron（`wrangler.jsonc` の `env.demo`）で毎日リセットされる。
実体は `workers/app.ts` の `scheduled` で、消すのは業務データだけ。
ログイン情報を消すと見学者が入れなくなるため残している。

## ステータス

MVP 実装中。

| 機能 | 状態 |
|---|---|
| 部屋・駐車場一覧（空室の導出） | ✅ |
| 入居 / 更新 / 退居ワークフロー | ✅ |
| 完了時の自動化（次回更新手続きの生成など） | ✅ |
| ホーム画面（やること・空室・今月の予定・放置検知） | ✅ |
| 修繕案件の登録・編集（放置検知つき） | ✅ |
| 部屋詳細・退居手続きの開始・募集内容の入力 | ✅ |
| 認証（招待制・ロール） | ✅ |

## セットアップ

Node 22.18 以上（シード生成が TypeScript を直接読むため）。

```bash
npm install
cp .dev.vars.example .dev.vars   # BETTER_AUTH_SECRET を設定
npm run db:migrate               # ローカル D1 にスキーマを適用
npm run db:seed                  # 架空のデモデータを投入
npm run dev
```

初回は `/signup` から最初のアカウント（管理者）を作成する。
以降のユーザーは設定画面から発行する招待リンク経由でのみ登録できる。

ローカルのアカウントを作り直したいときは次を実行する。
実行後は再び `/signup` で初回セットアップができる（業務データは消えない）。

```bash
npm run db:reset-auth
```

---

本リポジトリに実データは含まれない。デモ・開発用のシードデータはすべて架空のもの。
